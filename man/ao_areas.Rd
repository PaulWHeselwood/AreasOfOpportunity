% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/areas.R
\name{ao_areas}
\alias{ao_areas}
\title{List or search UK areas at a given level}
\usage{
ao_areas(
  level,
  name = NULL,
  code = NULL,
  within_level = NULL,
  within_code = NULL
)
}
\arguments{
\item{level}{One of \code{\link[=ao_area_levels]{ao_area_levels()}}.}

\item{name}{Optional substring to match against area names
(case-insensitive). Not supported for \code{"output_area"}, which has no
names.}

\item{code}{Optional substring to match against area codes
(case-insensitive).}

\item{within_level}{Optional coarser level in \code{\link[=ao_area_levels]{ao_area_levels()}} to
restrict results to.}

\item{within_code}{Required alongside \code{within_level}: the code of the
specific \code{within_level} area to restrict to.}
}
\value{
A tibble with (at least) \code{code} and \code{name} columns for matching
areas, plus one \verb{<ancestor>_code}/\verb{<ancestor>_name} column pair per
coarser level it's traceable to.
}
\description{
Areas are fetched from the ONS Open Geography Portal's lookup tables the
first time any \code{ao_areas()}/\code{\link[=ao_area_boundary]{ao_area_boundary()}} call needs them, and
cached locally afterwards (see \code{\link[=ao_clear_cache]{ao_clear_cache()}}).
}
\examples{
\dontrun{
ao_areas("lad", name = "Leeds")
leeds_code <- ao_areas("lad", name = "Leeds")$code[1]
ao_areas("ward", within_level = "lad", within_code = leeds_code)
}
}
